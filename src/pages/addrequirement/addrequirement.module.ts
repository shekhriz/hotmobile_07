import { NgModule } from '@angular/core';
import { IonicPageModule } from 'ionic-angular';
import { AddrequirementPage } from './addrequirement';

@NgModule({
  declarations: [
    AddrequirementPage,
  ],
  imports: [
    IonicPageModule.forChild(AddrequirementPage),
  ],
})
export class AddrequirementPageModule {}
